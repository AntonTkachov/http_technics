== HTTP technics in ruby on rails

This is a simple chat application demo, which gives user a possibility to
choose which 'live updates' technic he wants to use. The available technics
are HTTP short polling, HTTP long polling, HTTP streaming and Websockets
